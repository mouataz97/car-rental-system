RewriteEngine On

# If file or folder exists, load it
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Otherwise redirect everything to index.php
RewriteRule ^(.*)$ index.php?url=$1 [L,QSA]